
# Autonomous Backup Orchestrator (ABO) 🛡️

Autonomous Backup Orchestrator (ABO) — это прототип системы интеллектуальной оркестрации резервного копирования, разработанный для защиты данных от атак типа Ransomware (вирусов-шифровщиков).

Система реализует стратегию 3-2-1 и обеспечивает изоляцию хранилищ через зашифрованные SSH-тоннели.


## 🚀 Основные возможности

- Оркестрация «на лету»: Сжатие и шифрование данных происходят в процессе передачи, не оставляя временных файлов на диске.
- Шифрование AES-256: Использование GPG для надежной защиты бэкапов.
- Архитектура изоляции: Источник данных (VM1) и Хранилище (VM2) не имеют прямого доступа друг к другу.
- Автономность: Работа по расписанию или команде без участия оператора (через SSH-ключи).


## 🛠 Технологический стек

 - Язык: [Python 3](https://www.python.org/downloads/)
 - OC: [Ubuntu Server 22.04 LTS (Виртуальные машины в UTM/VMware)](https://ubuntu.com/download/server)
 - Протоколы: [SSH, SFTP](https://www.ssh.com/academy/ssh/sftp-ssh-file-transfer-protocol)
 - Инструменты: [GnuPG (GPG)](https://www.gnupg.org/)



## 📋 Схема развертывания


## 1. Требования

- VM1 (Agent): Сервер с рабочими данными (например, 192.168.64.6).

- VM2 (Storage): Удаленное хранилище (например, 192.168.64.7).

- Orchestrator: Ваш рабочий компьютер (Mac/Linux), управляющий процессом.


## 2. Подготовка окружения

На обеих виртуальных машинах должен быть установлен и запущен SSH-сервер:

```bash
  sudo apt update && sudo apt install openssh-server gnupg -y
sudo systemctl enable --now ssh
```
Настройка беспарольного доступа (SSH Keys)

```bash
 ssh-keygen -t ed25519 -N ""
ssh-copy-id user@<ip address>
ssh-copy-id user@<ip address>
```


## 3. Запуск системы

ABO логика:

```Python
import os
import datetime
import sys

# ==========================================
# КОНФИГУРАЦИЯ СИСТЕМЫ ABO
# ==========================================
SOURCE_VM = "knst@192.168.64.6"    # VM1: Источник данных
STORAGE_VM = "knst@192.168.64.7"   # VM2: Хранилище бэкапов

SOURCE_PATH = "~/my_data"         
GPG_PASSPHRASE = "ABO_Strong_2025" # Пароль для AES-256
# ==========================================

def run_orchestration():
    print(f"\n{'='*45}")
    print("🚀 AUTONOMOUS BACKUP ORCHESTRATOR (ABO) v1.0")
    print(f"{'='*45}")

    # 1. Генерация имени файла
    timestamp = datetime.datetime.now().strftime("%Y-%m-d_%H-%M-%S")
    filename = f"abo_encrypted_backup_{timestamp}.tar.gz.gpg"
    
    print(f"[*] Цель: {SOURCE_VM}:{SOURCE_PATH}")
    print(f"[*] Назначение: {STORAGE_VM}:~/backups/")

    # 2. Проверка доступности VM1 (Агента)
    print("\n[1/3] Проверка связи с Агентом (VM1)...")
    check_source = os.system(f"ssh -o ConnectTimeout=5 {SOURCE_VM} 'ls {SOURCE_PATH} > /dev/null'")
    if check_source != 0:
        print("❌ ОШИБКА: VM1 недоступна или папка не существует!")
        return

    # 3. Подготовка хранилища на VM2
    print("[2/3] Подготовка изолированного хранилища (VM2)...")
    os.system(f"ssh {STORAGE_VM} 'mkdir -p ~/backups'")

    # 4.Сжатие + Шифрование + Передача
    # Данные шифруются ДО того, как попадут в сеть или на вторую ВМ
    print("[3/3] Запуск шифрования (AES-256) и передачи данных...")
    
    # Конвейер: tar (сжать) -> gpg (зашифровать) -> ssh (передать)
    backup_cmd = (
        f"ssh {SOURCE_VM} 'tar -cz -C {SOURCE_PATH} . | "
        f"gpg --batch --yes --passphrase {GPG_PASSPHRASE} -c' | "
        f"ssh {STORAGE_VM} 'cat > ~/backups/{filename}'"
    )
    
    status = os.system(backup_cmd)

    if status == 0:
        print(f"\n✅ УСПЕХ: Данные оркестрованы и защищены.")
        print(f"📎 Файл на VM2: ~/backups/{filename}")
        
        # правило 3-2-1: Копия на Orchestrator (Mac)
        print(f"📦 Создание локальной избыточной копии на Mac...")
        os.system(f"scp {STORAGE_VM}:~/backups/{filename} ./local_copy_{filename}")
        print(f"📂 Локальный путь: {os.getcwd()}/local_copy_{filename}")
    else:
        print("\n❌ КРИТИЧЕСКИЙ СБОЙ: Поток данных прерван. Проверьте SSH-ключи!")

if name == "__main__":
    try:
        run_orchestration()
    except KeyboardInterrupt:
        print("\n🛑 Процесс прерван пользователем.")
        sys.exit()
```
Отредактируйте конфиг в начале файла abo_orchestrator.py, указав свои IP и пути.

Запустите оркестрацию:
```bash
 python3 abo_orchestrator.py
```
    
## 3. 🧪 Демонстрация восстановления
Для восстановления данных из зашифрованной копии используйте следующую команду на стороне Оркестратора:
```bash
 gpg -d --batch --passphrase ВАШ_ПАРОЛЬ файл.gpg | ssh user@IP_VM1 'tar -xz -C /путь/восстановления'
```


## 📄 Лицензия

Проект разработан в учебных целях в рамках проектной деятельности
